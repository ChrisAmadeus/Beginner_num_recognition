输入：
要检测的图片放在numbers文件夹下 自动读取所有文件

输出：
名字+识别出的数字 输出在控制台和result.txt里

调试：
detect.py供调试用，main.py供正式工作用
detect最后三行显示截取的区域、二值图，供调试用，不想影响速度可以把
52-53,60-63行删掉

显示图片：
红框对应可能是数字的区域

算法：
截取上1/2,右1/4的部分->
获取轮廓->
按大小(w,h)和形状(w:h)筛选，去掉太大（可能是黑边）太小（杂点）形状不符（汉字）的->
模版匹配，匹配度>0.5视为数字
