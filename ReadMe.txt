输入：
在目录下创建input.txt，每一行是一张图片的名字
要检测的图片放在numbers文件夹下

输出：
名字+识别出的数字

调试：
最后三行显示截取的区域、二值图，供调试用，不想影响速度可以把
52-53,60-63行删掉

显示图片：
红框对应可能是数字的区域

算法：
截取上1/3,右1/4的部分->
获取轮廓->
按大小(w,h)和形状(w:h)筛选，去掉太大（可能是黑边）太小（杂点）形状不符（汉字）的->
模版匹配，匹配度>0.5视为数字